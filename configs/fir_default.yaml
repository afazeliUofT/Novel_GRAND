experiment_name: fir_tags_grand_default
output_root: outputs
system:
  device: cpu
  num_workers: 64
  torch_threads_per_worker: 1
  seed: 20260411
nr:
  k: 1088
  n: 1344
  bits_per_symbol: 4
  num_ofdm_symbols: 14
  fft_size: 24
  cyclic_prefix_length: 0
  subcarrier_spacing_hz: 30000.0
  num_guard_carriers: [0, 0]
  dc_null: false
channel:
  mode: tdl
  delay_profile: C300
  delay_spread_s: 3.0e-07
  carrier_frequency_hz: 3500000000.0
  min_speed_mps: 2.0
  max_speed_mps: 8.33
  perfect_csi_equalization: true
  eps_equalizer: 1.0e-08
simulation:
  ebn0_db_list: [10.0, 11.0, 12.0, 13.0, 14.0]
  train_frames_per_worker_per_snr: 240
  eval_frames_per_worker_per_snr: 250
  sampled_failure_traces_per_worker_per_snr: 25
legacy_ldpc:
  num_iter: 12
  cn_update: minsum
  cn_schedule: layered
  llr_max: 8.0
  early_stop: true
grand:
  topk_bits: 96
  query_cap: 5000
  keep_oracle_upper_bound: true
  rescue_bonus_cap: 2000
  fallback_bonus_cap: 1500
  total_capmatched_budget: 8500
  active_teacher_cap: 8500

  # Active-set exact solver (publication candidate)
  astar_snapshot_candidates: 2
  astar_topk_bits: 14
  astar_active_set_cap: 22
  astar_unsat_groups: 8
  astar_keep_per_syndrome: 8
  astar_top_candidates: 16
  astar_accept_gap: 0.15
  astar_tta_temps: [0.80, 1.00, 1.25]
  astar_tta_probe_topk: 16
training:
  snapshot_hidden_dims: [128, 64]
  bit_hidden_dims: [128, 96]
  epochs: 24
  batch_size: 2048
  learning_rate: 0.001
  weight_decay: 1.0e-05
  validation_fraction: 0.1
report:
  query_quantiles: [0.5, 0.9, 0.99]
