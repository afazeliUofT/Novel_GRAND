experiment_name: fir_tags_grand_default
output_root: outputs
system:
  device: cpu
  num_workers: 64
  torch_threads_per_worker: 1
  seed: 20260411
nr:
  k: 1088
  n: 1344
  bits_per_symbol: 4
  num_ofdm_symbols: 14
  fft_size: 24
  cyclic_prefix_length: 0
  subcarrier_spacing_hz: 30000.0
  num_guard_carriers: [0, 0]
  dc_null: false
channel:
  mode: tdl
  delay_profile: C300
  delay_spread_s: 3.0e-07
  carrier_frequency_hz: 3500000000.0
  min_speed_mps: 2.0
  max_speed_mps: 8.33
  perfect_csi_equalization: true
  eps_equalizer: 1.0e-08
simulation:
  ebn0_db_list: [10.0, 11.0, 12.0, 13.0, 14.0]
  train_frames_per_worker_per_snr: 240
  eval_frames_per_worker_per_snr: 250
  sampled_failure_traces_per_worker_per_snr: 25
legacy_ldpc:
  num_iter: 12
  cn_update: minsum
  cn_schedule: layered
  llr_max: 8.0
  early_stop: true
grand:
  topk_bits: 96
  individual_init_width: 48
  query_cap: 5000
  max_primitives_in_pattern: 10
  max_group_count: 20
  search_expand_width: 10
  overlap_penalty: 0.35
  group_bonus_log_size: 0.1
  keep_oracle_upper_bound: true
  rescue_bonus_cap: 2000
  fallback_bonus_cap: 1500
  ai_teacher_cap: 2500

  # New GFlow/TTA rescue path.
  gflow_snapshot_candidates: 2
  gflow_primary_budget_fraction: 0.80
  gflow_shortlist_topk_bits: 96
  gflow_policy_pool_bits: 24
  gflow_beam_width: 8
  gflow_max_depth: 10
  gflow_expand_width: 8
  gflow_stochastic_expand: 2
  gflow_max_pairs_per_state: 4
  gflow_pair_seed_bits: 8
  gflow_top_complete_candidates: 12
  gflow_value_weight: 0.45
  gflow_llr_bias_weight: 0.25
  gflow_depth_penalty: 0.03
  gflow_tta_topn: 4
  gflow_tta_strength: 1.00
  gflow_seed_offset: 31
  gflow_local_repair_bits: 20
  gflow_local_repair_expand_width: 12
  gflow_local_repair_max_toggles: 3
  gflow_neg_to_pos_ratio: 6
  gflow_value_negatives_per_prefix: 3
training:
  snapshot_hidden_dims: [128, 64]
  bit_hidden_dims: [128, 96]
  action_hidden_dims: [128, 96]
  value_hidden_dims: [128, 96]
  epochs: 24
  batch_size: 2048
  learning_rate: 0.001
  weight_decay: 1.0e-05
  validation_fraction: 0.1
report:
  query_quantiles: [0.5, 0.9, 0.99]
